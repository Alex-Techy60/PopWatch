// src/features/video/videoSlice.js
// Placeholder for standard RTK state shape
import { createSlice } from '@reduxjs/toolkit';
export default createSlice({ name: 'video', initialState: {}, reducers: {} }).reducer;